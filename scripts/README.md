# Scripts

预留构建/发布脚本目录. M1 阶段会加入:

- `bundle-models.sh` - 把 shared/ 中的 glTF 拷贝到鸿蒙 rawfile 与桌面 assets
- `validate-skin-schema.sh` - 用 ajv 校验 SkinProfile 实例
- `validate-schedule-schema.sh` - 校验 DailySchedule 实例
